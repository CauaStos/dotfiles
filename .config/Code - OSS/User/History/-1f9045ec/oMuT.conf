general {
    lock_cmd = notify-send "lock!"          # dbus/sysd lock command (loginctl lock-session)
    unlock_cmd = notify-send "unlock!"      # same as above, but unlock
    before_sleep_cmd = notify-send "Zzz"    # command ran before sleep
    after_sleep_cmd = hyprctl keyword monitor DP-3,1920x1080@144,-1920x0,1  # command ran after sleep
    ignore_dbus_inhibit = true             # whether to ignore dbus-sent idle-inhibit requests (used by e.g. firefox or steam)
    ignore_systemd_inhibit = true          # whether to ignore systemd-inhibit --what=idle inhibitors
}

listener {
    timeout = 5                          # in seconds
    on-timeout = ~/Documents/Scripts/monitor_sleep.sh # command to run when timeout has passed
    on-resume = "we estar de volta" # command to run when activity is detected after timeout has fired.
}